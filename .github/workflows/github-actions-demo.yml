name: My First Test Action

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-22.04
    steps:
      - name: Echo 
        run: echo "Hello World"
      - name: Print Version
        run: 
          node -v
          npm -v